# Shared baseline — kept identical across all stock-* repos, see CLAUDE.md
.env
.env.local
.claude/settings.local.json

# Repo-specific — Node/Vite
node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo
